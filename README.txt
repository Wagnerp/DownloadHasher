DOWNLOADHASHER

Calculates hashsums of files added in a folder (Downloads folder)

Supports MD5, RIPEMD160, SHA1, SHA256, SHA384, SHA512

Displays notifications in the Windows notification bar when hash sums are calculated

Future Plans
-Reduce bugs with FileSystemWatcher
-Add user options (currently only changeable via code)
	-Montiored folder
	-Notifications
	-Hash sum (default is MD5 and SHA1)